{
  "name": "personal-ai-browser-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium --no-shell"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
